{
  "name": "guard_patterns",
  "version": "1.1.0",
  "description": "AST rules: no bare-except/debugger/eval. Custom via .ai-patterns.json. Pre-commit gate with dep_gatekeeper.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "file": {
        "type": "string",
        "description": "File to check (relative to workspace_dir)"
      },
      "ruleset": {
        "type": "string",
        "description": "Optional ruleset path relative to workspace_dir (default: .ai-patterns.json + built-in)"
      }
    },
    "required": [
      "workspace_dir",
      "file"
    ],
    "additionalProperties": false
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "lint",
    "rules",
    "ast",
    "guard"
  ],
  "dependencies": []
}
