{
  "name": "dep_gatekeeper",
  "version": "1.0.0",
  "description": "Check if code imports are declared in project dependency manifests. Supports pyproject.toml, package.json, requirements.txt, go.mod, Cargo.toml. Monorepo-aware.",
  "handler": "handler.js",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "Project root absolute path"
      },
      "file": {
        "type": "string",
        "description": "File to check (relative to workspace_dir)"
      }
    },
    "required": ["workspace_dir", "file"],
    "additionalProperties": false
  }
}
