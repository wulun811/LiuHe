{
  "name": "exception_guard",
  "version": "1.0.0",
  "description": "Check if raise/throw uses project custom exceptions vs builtins. Index optional (regex fallback for exception hierarchy). After fixing, verify with test_bridge(action=\"run\"). Pairs with config_drift as quality gate.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": { "type": "string", "description": "REQUIRED: Absolute path to the project root directory. Index optional (regex fallback)." },
      "file": { "type": "string", "description": "File to check (relative to workspace_dir)" }
    },
    "required": ["workspace_dir", "file"]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": ["quality", "exceptions"],
  "dependencies": ["reindex"]
}
