{
  "name": "trace_symbol",
  "version": "1.1.0",
  "description": "Trace constant values + hardcoded copies (magic numbers). Function impact? impact_analysis. Rename? rename_symbol.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "symbol": {
        "type": "string",
        "description": "Symbol name to trace (e.g. \"MAX_RETRY_COUNT\")"
      },
      "file": {
        "type": "string",
        "description": "File rel to root where symbol is defined"
      },
      "include_literals": {
        "type": "boolean",
        "description": "Search for hardcoded literal values across the project (default: false, slower)"
      },
      "max_results": {
        "type": "number",
        "description": "Max results per section (default: 30)"
      }
    },
    "required": [
      "workspace_dir",
      "symbol",
      "file"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "trace",
    "references",
    "literals"
  ],
  "dependencies": [
    "reindex"
  ]
}
