{
  "name": "naming_consistency",
  "version": "1.1.0",
  "description": "New symbol names vs project style (snake/camel/Pascal) + verb consistency. Index required.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "file": {
        "type": "string",
        "description": "Target file for language detection (relative to workspace_dir)"
      },
      "new_symbols": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "REQUIRED: array of new symbol names to check (e.g. [\"getUser\", \"queryAll\"])"
      },
      "lang": {
        "type": "string",
        "description": "Optional: python|javascript|typescript|go|rust|java (default: by extension)"
      }
    },
    "required": [
      "workspace_dir",
      "file",
      "new_symbols"
    ],
    "additionalProperties": false
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "naming",
    "style",
    "consistency"
  ],
  "dependencies": [
    "reindex"
  ]
}
