{
  "name": "naming_consistency",
  "version": "1.1.0",
  "description": "New symbols vs project naming: style (snake/camel/Pascal) + verb consistency. Index required. Pairs with guard_patterns before commit. Returns suggestions.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: Absolute path to the project root directory. Call reindex first."
      },
      "file": {
        "type": "string",
        "description": "Target file for language detection (relative to workspace_dir)"
      },
      "new_symbols": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "New function/class/variable names to check"
      },
      "lang": {
        "type": "string",
        "description": "Optional: python|javascript|typescript|go|rust|java (default: by extension)"
      }
    },
    "required": [
      "workspace_dir",
      "file",
      "new_symbols"
    ],
    "additionalProperties": false
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "naming",
    "style",
    "consistency"
  ],
  "dependencies": [
    "reindex"
  ]
}
