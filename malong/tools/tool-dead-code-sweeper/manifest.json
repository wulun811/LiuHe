{
  "name": "sweep_dead_code",
  "version": "1.0.0",
  "description": "Detect dead code: unused imports/functions, orphan files, unused_guard signal. Text-reference fallback counts CLI-string refs as alive (miss-over-delete bias). Remove via edit_transaction. Out of scope: dynamic/reflection wiring.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs)."
      },
      "scope": {
        "type": "string",
        "description": "Scan scope: directory relative to workspace_dir (default: \".\")"
      },
      "include_files": {
        "type": "boolean",
        "description": "Also detect orphan files (default: false)"
      }
    },
    "required": [
      "workspace_dir"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "quality",
    "dead-code"
  ],
  "dependencies": [
    "reindex"
  ]
}
