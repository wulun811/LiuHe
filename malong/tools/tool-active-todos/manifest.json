{
  "name": "active_todos",
  "version": "1.0.0",
  "description": "Scan TODO/FIXME/XXX/HACK, prioritize by current work. Pure regex, no index. Pass current_files to boost.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "scope": {
        "type": "string",
        "description": "Scan scope: directory or file path relative to workspace_dir (default: \".\")"
      },
      "current_files": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Files currently being edited (boosts priority)"
      }
    },
    "required": [
      "workspace_dir"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "quality",
    "todo"
  ],
  "dependencies": []
}
