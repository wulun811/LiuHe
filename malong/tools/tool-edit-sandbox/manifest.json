{
  "name": "sandbox_validate",
  "version": "1.0.0",
  "description": "Pre-validate edits: pass file + new_content (full new text), dry-run syntax check. Then edit_transaction.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "file": {
        "type": "string",
        "description": "Target file path (relative to workspace_dir)"
      },
      "new_content": {
        "type": "string",
        "description": "Full file content after edits (or the edited section)"
      }
    },
    "required": [
      "workspace_dir",
      "file",
      "new_content"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "quality",
    "validation"
  ],
  "dependencies": []
}
