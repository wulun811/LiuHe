{
  "name": "rename_symbol",
  "version": "1.0.0",
  "description": "Atomic cross-file symbol rename. Finds all references, applies word-boundary-safe replacement via edit_transaction. dry_run=true by default.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": { "type": "string", "description": "REQUIRED: Absolute path to the project root directory. Call reindex first." },
      "symbol": { "type": "string", "description": "Current symbol name to rename" },
      "new_name": { "type": "string", "description": "New symbol name" },
      "file": { "type": "string", "description": "File where the symbol is defined (relative to workspace_dir)" },
      "dry_run": { "type": "boolean", "description": "Preview changes without writing (default: true)" }
    },
    "required": ["workspace_dir", "symbol", "new_name", "file"]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": ["refactor", "rename"],
  "dependencies": ["reindex"]
}
