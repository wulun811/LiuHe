{
  "name": "spec_gen",
  "version": "1.0.0",
  "description": "Generate module/API spec from source symbols; returns in-memory spec, never writes files.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "file": {
        "type": "string",
        "description": "Source file (relative to workspace_dir) to generate spec for"
      }
    },
    "required": [
      "workspace_dir",
      "file"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "spec",
    "doc"
  ],
  "dependencies": [
    "reindex"
  ]
}
