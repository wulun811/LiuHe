{
  "name": "reindex",
  "version": "0.0.5",
  "description": "Index workspace for all malong tools. MUST call first for new workspaces. blocking/force options.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "Project root (abs). Omit to check indexing status."
      },
      "blocking": {
        "type": "boolean",
        "description": "If true, wait for indexing to complete before returning (default false = async)."
      },
      "force": {
        "type": "boolean",
        "description": "Mark ALL files dirty + re-extract (ignore mtime). Use to purge stale index. Default: incremental."
      },
      "maxFiles": {
        "type": "number",
        "description": "Max files to index (default 5000, 0=unlimited). Increase if project has many files."
      },
      "skipDirs": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Skip specific directories by relative path, e.g. [\"runtime1\", \"0通天/eval-cases\"]. Useful when pre-check says too many files."
      },
      "ignoreDirs": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Additional directory names to skip globally (e.g. [\"deps\", \"archive\"]). Combined with built-in defaults."
      },
      "threshold": {
        "type": "number",
        "description": "File count threshold for pre-check review (default 2000). Set higher to skip pre-check."
      }
    }
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "index",
    "init"
  ],
  "dependencies": []
}
