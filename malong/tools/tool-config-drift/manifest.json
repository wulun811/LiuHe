{
  "name": "config_drift",
  "version": "1.0.0",
  "description": "Detect env vars/DB tables/services in code missing from .env.example/docker-compose. Pairs with exception_guard.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "file": {
        "type": "string",
        "description": "File to check (relative to workspace_dir). Omit to scan all source files."
      }
    },
    "required": [
      "workspace_dir"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "quality",
    "config"
  ],
  "dependencies": []
}
