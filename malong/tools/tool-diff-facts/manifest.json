{
  "name": "diff_facts",
  "version": "1.1.0",
  "description": "After edit_transaction: AST symbol changes + caller/test sync. Stale tests? test_bridge.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs). reindex first if new."
      },
      "since": {
        "type": "string",
        "description": "\"last_txn\" (default) or \"txn:<id>\" (get id from edit_transaction action=begin)"
      }
    },
    "required": [
      "workspace_dir"
    ],
    "additionalProperties": false
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "diff",
    "transaction",
    "ast"
  ],
  "dependencies": [
    "edit_transaction"
  ]
}
