{
  "name": "health",
  "version": "1.2.0",
  "description": "System check + self-healing: DB integrity, tools, memory, semaphore. actions: check/restart/cleanup.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "check (default) / restart (soft-recover) / cleanup (prune stale caches)"
      },
      "stats": {
        "type": "boolean",
        "description": "Usage stats: calls, success rate, tokens saved"
      },
      "max_age_days": {
        "type": "number",
        "description": "For action='cleanup': prune workspace caches not accessed within this many days (default 14)."
      },
      "dry_run": {
        "type": "boolean",
        "description": "For action='cleanup': report what would be pruned without deleting (default false)."
      }
    },
    "required": []
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "system",
    "health",
    "self-healing"
  ]
}
