{
  "name": "gc",
  "description": "Trigger manual GC (needs --expose-gc). Use when memory is high.",
  "handler": "handler.js",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  }
}
