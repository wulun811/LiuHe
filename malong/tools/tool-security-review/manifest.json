{
  "name": "security_review",
  "version": "1.0.0",
  "description": "Scan for security patterns: injection (eval/exec/spawn/SQL/template), XSS, secrets, CORS*, insecure compare. Severity-graded, zero LLM. Suppress via malong-ignore or .ai-patterns.json. Out of scope: SSRF/XXE/deserialization/auth.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: absolute path of the project root to scan"
      },
      "file": {
        "type": "string",
        "description": "File path relative to workspace_dir to scan (reads from disk)"
      },
      "source": {
        "type": "string",
        "description": "Source code text to scan (mutually exclusive with file)"
      },
      "scope": {
        "type": "string",
        "description": "Directory relative to workspace_dir to scan recursively (default: none, single file only). scope wins over file/source if both passed"
      },
      "max_findings": {
        "type": "number",
        "description": "Max findings per file to return (default: 50)"
      }
    },
    "required": [
      "workspace_dir"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "security",
    "review"
  ],
  "dependencies": []
}
