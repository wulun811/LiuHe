{
  "name": "tsc_check",
  "version": "1.0.0",
  "description": "TS type-check via tsc --noEmit -> {status,errorCount}; timeout 60s default; dir = subdir.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "workspace_dir": {
        "type": "string",
        "description": "REQUIRED: project root (abs)."
      },
      "dir": {
        "type": "string",
        "description": "subdir"
      },
      "timeout": {
        "type": "number",
        "description": "tsc run timeout in ms (default 60000)"
      }
    },
    "required": [
      "workspace_dir"
    ]
  },
  "handler": "./handler.js",
  "protocol_version": "1.0",
  "tags": [
    "tsc",
    "typecheck"
  ],
  "dependencies": []
}
